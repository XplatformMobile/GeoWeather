// Default code
/*
".container" : {
  
}
*/

"#addLocationListItemWin":{
	layout: 'vertical',
	barColor: '#a00'
}

// TODO: Fix so that it properly shows both text fields or create separate ids/styles for them
".textField":{
	width: '90%',
	top: '25dp',
	borderStyle: Ti.UI.INPUT_BORDERSTYLE_ROUNDED,
	returnKeyType: Ti.UI.RETURNKEY_DONE
}

".button":{
	width:'50%',
	top:'20dp'
}
